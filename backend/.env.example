# ============================================================================
# Election System - Environment Configuration File
# ============================================================================
# This file contains sensitive configuration. Never commit this to git!
# Copy this file to .env and fill in your actual values.
# ============================================================================

# ============================================================================
# Gmail SMTP Configuration (Required for OTP Email Functionality)
# ============================================================================

# Gmail SMTP Server
MAIL_SERVER=smtp.gmail.com

# Gmail SMTP Port (587 for TLS, 465 for SSL)
MAIL_PORT=587

# Your Gmail address (the email address that sends OTPs)
# Example: myemail@gmail.com
MAIL_USERNAME=your-email@gmail.com

# App Password from Google Account Settings (NOT your regular Gmail password)
# Generate this at: https://myaccount.google.com/ > Security > App passwords
# It will be a 16-character password
MAIL_PASSWORD=xxxx xxxx xxxx xxxx

# Email address that appears in the "From" field of sent emails
# Can be the same as MAIL_USERNAME
MAIL_DEFAULT_SENDER=your-email@gmail.com

# ============================================================================
# Flask Security Configuration
# ============================================================================

# Secret key for Flask session management (change this!)
SECRET_KEY=super-secret-key-for-development

# JWT secret key for authentication (change this!)
JWT_SECRET_KEY=super-secret-key-for-jwt

# ============================================================================
# Frontend Configuration
# ============================================================================

# Frontend URL (where your React app is hosted)
FRONTEND_ORIGIN=http://localhost:3000

# ============================================================================
# Encryption Keys
# ============================================================================

# Encryption key for vote data (Fernet format)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key())"
ENCRYPTION_KEY=your-encryption-key-here

# Homomorphic encryption keys
HE_PUBLIC_KEY=your-he-public-key
HE_PRIVATE_KEY=your-he-private-key

# ============================================================================
# MongoDB Configuration (Optional)
# ============================================================================

# MongoDB connection string (optional, comment out to disable)
# Example: mongodb+srv://username:password@cluster.mongodb.net/
# MONGODB_URI=mongodb+srv://yogeshmagatam_db_user:0yS9slaSGAcNZ0NP@cluster116454.sypvlt0.mongodb.net/

# MongoDB database name
# MONGODB_DB_NAME=election_db

# ============================================================================
# Feature Flags (Development Mode)
# ============================================================================

# Allow automatic face enrollment during first login (development only)
# Set to 'false' in production
ALLOW_AUTO_FACE_ENROLLMENT=true

# Allow bypassing ID verification (development only)
# Set to 'false' in production
ALLOW_ID_VERIFICATION_BYPASS=true

# Allow bypassing strict voter master list validation (development only)
# Set to 'false' in production
ALLOW_VOTER_VALIDATION_BYPASS=true

# ============================================================================
# Security Settings
# ============================================================================

# Account lockout duration in minutes (after 5 failed login attempts)
ACCOUNT_LOCKOUT_DURATION=30

# Password expiration days (0 to disable)
PASSWORD_EXPIRATION_DAYS=90

# ============================================================================
# Important Notes
# ============================================================================
#
# 1. Gmail App Password Setup:
#    - Go to https://myaccount.google.com/
#    - Click Security (left menu)
#    - Enable 2-Step Verification if not already enabled
#    - Find "App passwords" near the bottom
#    - Select Mail and Windows/Linux
#    - Copy the 16-character password and paste here
#
# 2. Never share this file or commit it to version control
#
# 3. In production, use your hosting platform's environment variable storage:
#    - AWS: Parameter Store or Secrets Manager
#    - Heroku: Config Vars
#    - Azure: Key Vault
#    - Google Cloud: Secret Manager
#
# 4. Encryption keys should be strong random values:
#    Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key())"
#
# ============================================================================
